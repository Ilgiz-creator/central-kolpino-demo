# Проверка Central · 2026-09-11

- Production: `node validate.mjs` — успешно. Готовые статические HTML/CSS/WebP/SVG лежат в `dist/`; зависимостей и обязательного этапа компиляции нет. Начальная HTTP-проверка — 200.
- В браузере осмотрены первый экран, сценарии посещения, согласование мероприятия, контакты и подвал на 390×844, 820×1180 и 1440×1000. Иерархия, интервалы, изображение и контраст читаемы; горизонтального скролла нет (`scrollWidth === innerWidth`). Все изображения загрузились.
- Полностраничная склейка встроенного браузера давала повторяющиеся фрагменты и неверную ширину, поэтому итоговая визуальная оценка проведена по отдельным снимкам видимой области после переходов по разделам.
- Исправлены пропуски пробелов при скрытии `<br>` на мобильных, переносы длинных заголовков, переполнение при 200% размере текста. Для проверки временно удвоен корневой размер шрифта: на 390 px горизонтальный скролл отсутствует, меню переносится, текст остаётся доступным; тестовое правило удалено.
- Проверены якоря «Кафе», «Встречи и обеды», «Контакты», «Обсудить встречу», возврат наверх. Клавиатурные ссылки имеют явный фокус, есть skip-link. Настроен prefers-reduced-motion.
- Телефон, Telegram, WhatsApp и точный маршрут сверены с открытыми карточками. Телефон/мессенджеры не запускались на отправку: тестовых сообщений и звонков не было. Telegram открывался отдельно и подтвердил Central Кафе. На сайте нет формы, оплаты, бронирования с ложным подтверждением или клиентского JavaScript.
- Консоль браузера: ошибок и предупреждений нет. Локальные ресурсы и якоря проверены статическим валидатором; пропавших файлов нет.
- Название, адрес, график, 35 мест, кухня, виды обедов, телефон и рейтинг сверены с BRIEF.md. Цены и конкретные блюда не опубликованы. Отзывы не копируются.
- Только приватное демо. Неофициальный статус обозначен сверху и в подвале, иллюстрация подписана. Публичного запуска не было.
- Ограничение завершения: GitHub CLI недействителен; требуется восстановить вход, проверить отсутствие дубля и отправить этот проект в приватный репозиторий. До проверки удалённого GitHub SHA статус кандидата остаётся «Демо готовится».
